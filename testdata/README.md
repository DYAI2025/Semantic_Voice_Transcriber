# SVT Audit Testdata

Synthetic sessions for audit smoke tests.

- `audio/session1.wav`: 2s sine wave with amplitude change to simulate prosody shift.
- `transcripts/session1.json`: minimal transcript with 3 segments covering a turning point.

Regenerate via `python testdata/generate_sample_audio.py` if needed.
